<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.healthcentral.activity"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk android:minSdkVersion="7" />
    
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:icon="@drawable/hc_logo"
        android:label="@string/app_name" >
        <activity
            android:name=".SplashActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity android:name="com.healthcentral.activity.HealthCentralActivity" android:label="@string/app_name"/>
        <activity android:name=".SiteSlideshowsActivity" android:label="@string/app_name"/>
        <activity android:name="com.healthcentral.activity.SlideshowDetails" android:label="@string/app_name"/>
        <activity android:name=".SiteResourcesActivity" android:label="@string/app_name"/>
        <activity android:name=".SiteQuizzesActivity" android:label="@string/app_name"/>
    </application>

</manifest>